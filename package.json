{
  "name": "restful-api-database-integration",
  "version": "1.0.0",
  "description": "this project contain a RESTful API that integrates a database (NoSQL) to manage a collection of users and items (or any other relevant resource). The focus will be on CRUD operations and database connectivity using Mongoose for MongoDB.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  }
}
